# import socket


# server = socket.socket()

# server.connect(('localhost', 9090))

# data = None
# while True:
#     server.send(input('User_2: ').encode('utf-8'))
#     data = server.recv(1024).decode('utf-8')
#     print(f'Собеседник: {data}')
